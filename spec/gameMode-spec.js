"use strict";
describe('GameMode', function(){
	var game = null;

	beforeEach(function(){
		// game = new V3.Game();
		// game.mode.systems.map(function(system){
		// 	spyOn(system, 'init').and.callThrough();
		// });
		// // spyOn(game.mode, 'startPlay').and.callThrough();
		// game.run();
	});

	it('should init all systems', function(){
		// expect(game.mode.startPlay).toHaveBeenCalled();
		// game.mode.systems.map(function(system){
		// 	expect(system.init).toHaveBeenCalled();
		// });
	});
});
